<?xml version="1.0" encoding="utf-8" ?>
<ContentView 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="OmniFetch.App.Views.Controls.ToolbarButton"
    x:Name="ThisButton">

    <Border 
        x:Name="ButtonBorder"
        BackgroundColor="Transparent"
        Stroke="Transparent"
        StrokeThickness="1"
        Padding="6,4"
        Margin="2,0"
        WidthRequest="72">
        <Border.StrokeShape>
            <RoundRectangle CornerRadius="4" />
        </Border.StrokeShape>

        <Border.GestureRecognizers>
            <TapGestureRecognizer 
                Command="{Binding Command, Source={x:Reference ThisButton}}" 
                CommandParameter="{Binding CommandParameter, Source={x:Reference ThisButton}}" />
            <PointerGestureRecognizer 
                PointerEntered="OnPointerEntered" 
                PointerExited="OnPointerExited" />
        </Border.GestureRecognizers>

        <VerticalStackLayout Spacing="2" HorizontalOptions="Center" VerticalOptions="Center">
            <Image 
                Source="{Binding IconSource, Source={x:Reference ThisButton}}" 
                WidthRequest="28" 
                HeightRequest="28" 
                Aspect="AspectFit" 
                HorizontalOptions="Center" />
            <Label 
                Text="{Binding Text, Source={x:Reference ThisButton}}" 
                Style="{StaticResource IdmToolbarButtonLabel}" 
                HorizontalOptions="Center"
                LineBreakMode="NoWrap" />
        </VerticalStackLayout>
    </Border>

</ContentView>
